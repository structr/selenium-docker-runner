
define username "admin"
define password "admin"

run "snippets/init.test"

click text "Pages"

# open pages tree view
if not exists id "pagesTree"
        click text "Pages Tree View"

click id "add_page"
click text "Initial body text"
pause 1000
runScript "window.editor.setValue('${project.name}');"
click id "saveAndClose"

